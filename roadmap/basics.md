1. Fix the two bugs above (database fallback + error checking) -> done
2. Add requirements.txt with pinned versions -> Will be doing it manually 
3. Add .env.example documenting all required vars -> will be doing it manually 
4. Replace print() with logging
5. Add basic unit tests
6. Fix Python 3.8 type hint compatibility
7. Add GitHub Actions for lint + test on push
8. Expand README with setup/usage instructions
